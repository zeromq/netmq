namespace NetMQ.Core
{
    /// <summary>
    /// This enum-type is either IOThreads (1) or MaxSockets (2).
    /// </summary>
    internal enum ContextOption
    {
        IOThreads = 1,
        MaxSockets = 2
    }
}